parameters:
  - name: ProjectName
    description: Name of the project
    validate: '[a-zA-Z]+'

steps:
  - shell: echo Test project is ${{ ProjectName }}
  # - scaffold:
  #     package: github.com/azweb76/x-scaffold@master
  #     steps:
  #       - fetch:
  #           source: README.md
  #           templates:
  #           - path: README.md
  #             tokens:
  #               project: ${{ ProjectName }}
