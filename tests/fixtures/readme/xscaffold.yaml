parameters:
  - name: ProjectName
    description: Name of the project
    validate: '[a-zA-Z]+'

steps:
  - fetch:
      source: README.md
      templates:
        - path: README.md
          tokens:
            PROJECT_NAME: ${{ ProjectName }}