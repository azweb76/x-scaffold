parameters:
  - name: ProjectName
    description: Name of the project
    validate: '[a-zA-Z]+'

steps:
  - scaffold:
      package: github.com/azweb76/x-scaffold@master
      steps:
        - fetch:
            source: README.md
            templates:
            - path: README.md
              tokens:
                project: ${{ ProjectName }}
